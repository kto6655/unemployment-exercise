# this "app/__init__.py" file
# helps us import from one file to another

# after we refactor (e.g. API_KEY) and import code from one file to another
# we have to have to add this python file

# we also need to use the modular style commands to RUN the codes now
# python -m app.unemployment
# python -m app.stocks